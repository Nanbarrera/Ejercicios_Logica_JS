// Ejercicio 4
/*Condicionales: Crea un programa que verifique si un número es positivo,
negativo o cero, e imprima el resultado.*/

let n = prompt("El numero es:");

let verif = n < 0 ? "Negativo" : n > 0 ? "Positivo" : "Cero";

console.log(verif);
