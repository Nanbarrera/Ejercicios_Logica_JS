// Ejercicio 9
/*Arrays básicos: Declara un array con 5 nombres y muestra cada nombre en la
consola usando un ciclo.*/

// Declara un array con 5 nombres
const nombres = ["Nancy", "Michelle", "Elvin", "Joanna", "Olga"];

// Usa un ciclo for para mostrar cada nombre en la consola
for (let i = 0; i < nombres.length; i++) {
    console.log(nombres[i]); // Imprime el nombre en la consola
}
